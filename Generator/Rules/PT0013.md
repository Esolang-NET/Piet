# PT0013: Method must be partial

## Cause
The method decorated with `GeneratePietMethodAttribute` is not declared with the `partial` modifier.

## Solution
Add the `partial` modifier to the method definition.

## Example

```cs
partial class Sample
{
    // Incorrect: not partial
    [GeneratePietMethod("program.png")]
    public void Invalid();
}
```
