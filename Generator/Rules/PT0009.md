# PT0009: Duplicate image path mapping

## Cause
The Piet image path is mapped by multiple `AdditionalFiles` entries.

## Solution
Ensure only one unique image file is mapped to the image path used in the attribute.
