# PT0011: Return type and output parameter conflict

## Cause
The method cannot combine a string or byte-sequence return type with an explicit output parameter.

## Solution
Change the return type or remove the output parameter.
