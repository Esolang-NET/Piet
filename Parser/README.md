# Esolang.Piet.Parser

Core parsing primitives for Piet programs.

This package is intended to load Piet source images and normalize them into an in-memory program model that can be consumed by processors and tools.
